!uses <std.gfs>

!uses <cli.gfs>
!uses <file.gfs>

; sequnce - data type that it is a "array" 
; 
;
;

SEQ[10 BYTE] filename: @parse_cli
SEQ[10 BYTE] source_code: @load_file [ filename ]
@display [ source_code ]
@exit [ BYTE #0 ]
